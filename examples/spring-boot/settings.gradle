pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name="springboot-CRooD-example"

include ":generator"
project(":generator").projectDir = new File("../generator")
